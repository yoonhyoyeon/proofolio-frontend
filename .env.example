# Google Cloud Text-to-Speech browser API key.
# Create one in the Google Cloud console, enable the "Cloud Text-to-Speech API",
# and restrict the key to that API + your site's HTTP referrers.
# Copy this file to `.env.local` (git-ignored) and fill in the value.
VITE_GC_TTS_API_KEY=
